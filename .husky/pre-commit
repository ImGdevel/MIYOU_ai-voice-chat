#!/usr/bin/env sh

echo "🎨 코드 포맷팅 및 import 정리를 시작합니다..."

./gradlew spotlessApply

if [ $? -ne 0 ]; then
  echo "❌ 코드 포맷팅 실패"
  exit 1
fi

git add -u

echo "✅ 코드 포맷팅 완료"
